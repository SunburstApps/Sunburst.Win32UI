# Ignore cloned dependencies.
/*/
# Also ignore the .ignore file created by the update.py script.
/*.ignore

# But don't ignore these specific files.
# (The Atom editor has a problem handling the '*/' rule above,
# treating it as '*' instead, thereby hiding all of the contents
# of this directory. See <https://github.com/atom/tree-view/issues/902>)
!/dependencies.json
!/update.py
